# CDB location
# Comment this line if the CDB being used shouldn't be decompressed
# from a tar file.
CDB_PACKED_FILE=./config/CDB.tar.gz
TESTDIR=`pwd`
ACS_CDB=`pwd`/tmp
# ACS_CDB=`pwd`/config
ACS_TMP=`pwd`/tmp
ACS_LOCK=${ACS_TMP}/.running
ACS_INSTANCE=0
ACS_CONTAINERS="ACC/javaContainer OBOPS/ACC/javaContainer ACC/pythonContainer"
IDL_FILES_TO_LOAD="projectlifecycle.idl ProjectRepositoryIF.idl SchedulingArchiveUpdater.idl"

# Hibernate database configuration
DBDIR=$ACS_TMP/hsqldb
DBNAME=statearchive
DBNAME_SWDB=data_model
DBPORT=9001
SQLTOOL_RC_FILE=./config/sqltool.rc

# State system
RUNLOCATION=tst

JAVA_OPTIONS="-Darchive.configFile=$TESTDIR/config/archiveConfig.properties $JAVA_OPTIONS"
JAVA_OPTIONS="-DACS.managerhost=localhost $JAVA_OPTIONS"

# unset DISPLAY
